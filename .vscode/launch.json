{
    "configurations": [

        {
            "name": "Launch Program",
            "program": "${workspaceFolder}/app/server.js",
            "request": "launch",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "pwa-node"
        }
    ]
}